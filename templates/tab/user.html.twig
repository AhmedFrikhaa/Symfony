{% extends "template.html.twig" %}
{% block body %}
    {% block Pagetitle %}
        users table
    {% endblock %}
    {{ include('fragment/datejour.html.twig') }}

<table class="table">
    <tr class="table-active">
    <td>Firstname</td>
    <td>Name</td>
    <td>Age</td>
    <td>Message</td>
    </tr>
    {% for user in users %}
    <tr class="table-{% if loop.index is odd  %} alert-danger {% else %} alert-warning {% endif   %}">
        <td>{{ user.firstname }}</td>
        <td>{{ user.name }}</td>
        <td>{{ user.age }}</td>
        <td>{{ render(controller('App\\Controller\\TestduhelloController::index',{firstname:user.firstname,name:user.name})) }}</td>
    </tr>
    {% endfor %}
 </table>
{% endblock %}
{%  block title%}users page {% endblock %}
{% block navigation %}
Users Mode
 {% endblock %}
